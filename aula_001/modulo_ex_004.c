#include <stdio.h>

float volume(float u, i, j){
    return o=
}

int main(){
    float r, p=3.14, o;
    printf("Digite o raio da esfera");
    scanf("%f", &r);
    printf("%f",volume(r, p, o));
}